# dadam
DAO packet is where all direct comunication happens with the databse tries to keep all references to entites here as well
database packet where all the entities are they are formed from the database so be careful if you're going to change these
Bean packet where all comunication directly with the HTML are 
